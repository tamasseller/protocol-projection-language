# Dependencies
node_modules/

# Build output
dist/

# VS Code
.vscode/

# OS files
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
# Throwaway files written to disk purely so a generated module can be
# require()'d back in a test (any package's test/ dir) — distinct from a
# package's own committed generated/ output (e.g. packages/example/generated/).
**/test/.codegen-scratch/

# The MOG repos being split out of this tree — their own git, not this one's.
/mog-core/
/mog-jit/
