/**
 * Test entry point for the example.
 *
 * Runs every integration suite. Each proves that a GENERIC package
 * projection from `ppl`, when composed over this
 * project's schema via compose.ts, produces correct output.
 */
import "./codec.test"
import "./ts-target.test"
import "./js-codegen.test"
