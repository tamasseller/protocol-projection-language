{
  "name": "ppl-example",
  "version": "1.0.0",
  "private": true,
  "description": "A worked protocol: one semantic schema projected into a binary codec, a JSON encoder and TypeScript declarations.",
  "scripts": {
    "build": "tsc",
    "test": "ts-node --project tsconfig.test.json test/run.ts",
    "test:types": "tsc --project tsconfig.test.json --noEmit",
    "generate": "ts-node --project tsconfig.test.json src/generate.ts"
  },
  "dependencies": {
    "ppl": "file:..",
    "mog-core": "git+https://github.com/tamasseller/mog-core.git"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
