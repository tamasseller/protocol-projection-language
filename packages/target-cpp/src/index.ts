/**
 * @ppl/target-cpp — Barrel re-exports.
 *
 * C++ target emitter: generates idiomatic C++ type headers
 * and (future) zero-allocation codec functions from the
 * semantic type graph.
 */
export * from "./cpp-emitter"
