{
  "name": "@ppl/example",
  "version": "1.0.0",
  "private": true,
  "description": "Three-way split example: shared schema → C embedded + TS desktop",
  "scripts": {
    "build": "tsc",
    "test": "ts-node --project tsconfig.test.json test/run.ts",
    "test:types": "tsc --project tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@ppl/core": "*",
    "@ppl/machine": "*",
    "@ppl/codecs": "*",
    "@ppl/target-cpp": "*",
    "@ppl/target-js": "*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
