/**
 * @ppl/codecs — Barrel re-exports.
 *
 * IR builder (tagged template micro-parser), IR AST node definitions,
 * and codec primitives for encoding/decoding semantic types to/from
 * wire formats.
 */
export * from "./ir-builder"
export * from "./codecs"
export * from "./wire-format"
