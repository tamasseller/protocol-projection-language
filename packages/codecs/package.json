{
  "name": "@ppl/codecs",
  "version": "1.0.0",
  "description": "IR builder, AST definitions, and codec primitives",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "ts-node --project tsconfig.test.json test/run.ts",
    "test:types": "tsc --project tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@ppl/core": "*",
    "mog-core": "git+https://github.com/tamasseller/mog-core.git"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
