{
  "name": "declarative-serialization-compiler",
  "version": "1.0.0",
  "description": "Zero-allocation multi-pass serialization compiler",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "test": "ts-node --project tsconfig.test.json test/run-all.ts",
    "test:types": "tsc --project tsconfig.test.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
