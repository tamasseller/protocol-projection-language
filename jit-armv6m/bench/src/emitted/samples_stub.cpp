/* The input buffer ext_sampstream.cpp's emitters materialize the address of.
 * Only the link needs it here — nothing runs. */
#include "ext_sampstream.h"

const int16_t g_sampIn[SAMP_IN_SAMPLES] = {};
